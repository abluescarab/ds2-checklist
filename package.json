{
  "scripts": {
    "build": "tsc && tsc-alias",
    "build:watch": "tsc && (concurrently \"tsc -watch\" \"tsc-alias --watch\")"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsc-alias": "^1.8.11",
    "typescript": "^5.7.3"
  }
}
